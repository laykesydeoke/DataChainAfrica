name: Data Bounty Tests
on:
  push:
    branches: [feat/data-bounties, main]
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: npm run test:bounties
      - run: npm run test:bounty-edge
