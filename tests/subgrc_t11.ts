import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("subgrc 11",()=>{it("case 11",()=>{const r=simnet.callReadOnlyFn("billing","get-paused",[],simnet.deployer);expect(r.result).toBeDefined();});});
