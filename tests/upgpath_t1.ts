import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("upgpath 1",()=>{it("case 1",()=>{const r=simnet.callReadOnlyFn("billing","get-paused",[],simnet.deployer);expect(r.result).toBeDefined();});});
