import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("repdec 14",()=>{it("case 14",()=>{const r=simnet.callReadOnlyFn("marketplace","get-paused",[],simnet.deployer);expect(r.result).toBeDefined();});});
