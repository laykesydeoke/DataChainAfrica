import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("subgrc 16",()=>{it("case 16",()=>{const r=simnet.callReadOnlyFn("billing","get-paused",[],simnet.deployer);expect(r.result).toBeDefined();});});
